name: Token Authentication Test

on:
  workflow_dispatch:
    inputs:
      password:
        description: 'Test parameter (not used for validation)'
        required: false

jobs:
  validate-token:
    runs-on: ubuntu-latest
    steps:
      - name: 'Token Validation Test'
        run: |
          echo "✅ Token is valid - workflow triggered successfully"
          echo "Authentication happens via token permissions, not password validation"